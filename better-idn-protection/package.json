{
  "name": "better-idn-protection",
  "version": "1.0.0",
  "description": "Chrome extension to detect mixed-script IDN homograph attacks.",
  "engines": {
    "node": ">=14"
  },
  "scripts": {
    "test": "node test_mixed_scripts.js",
    "lint": "./node_modules/.bin/eslint . --ext .js --max-warnings=0 || true",
    "prepare": "echo 'prepare step - none'",
    "pack": "node -e \"const z=require('child_process').spawnSync('zip',['-r','extension.zip','.','-x','node_modules/**','.github/**','*.log']); console.log('Wrote extension.zip');\""
  },
  "devDependencies": {
    "eslint": "^8.50.0"
  },
  "license": "MIT"
}
